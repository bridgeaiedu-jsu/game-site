# Cloudflare Pages 응답 헤더 규칙.
# 기본 주소 hanpango.pages.dev 가 사이트 전체를 200 으로 그대로 보여 준다(2026-09-23 실측).
# canonical 이 hanpango.com 을 가리키지만, 같은 글이 두 주소에서 색인되지 않게 확실히 막는다.
# ★본 도메인(hanpango.com)에는 적용되지 않는다 — 주소 전체(호스트 포함)로 짝짓는 규칙이다.
https://hanpango.pages.dev/*
  X-Robots-Tag: noindex

https://:branch.hanpango.pages.dev/*
  X-Robots-Tag: noindex
